{
  "name": "cash-control",
  "version": "1.0.0",
  "repository": "",
  "license": "MIT",
  "author": "Jonathan Sapatieri",
  "description": "",
  "keywords": ["money", "cash", "control", "cash-control" ],
  "main": "server.js",
  "scripts": {
    "start": "SET NODE_ENV=prod&& node -r dotenv/config server.js dotenv_config_path=./src/config/.env",
    "start:dev": "SET NODE_ENV=dev&& nodemon -r dotenv/config server.js dotenv_config_path=./src/config/.env",
    "migrate": "SET NODE_ENV=prod&& node -r dotenv/config ./src/database/ dotenv_config_path=./src/config/.env",
    "migrate:dev": "SET NODE_ENV=dev&& node -r dotenv/config ./src/database/ dotenv_config_path=./src/config/.env"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "sqlite3": "^5.0.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "dotenv": "^8.2.0",
    "mocha": "^8.0.1",
    "nodemon": "^2.0.4",
    "supertest": "^4.0.2"
  }
}
